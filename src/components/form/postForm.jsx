import React from 'react';
import PropTypes from 'prop-types';

const PostForm = () => {
  return (
    <form>
      <input type="text" placeholder="post title" value={} onChange />
      <input type="text" placeholder="add post" value={} onChange />
      <button>Submit Post</button>
    </form>
  )
};

export default PostForm;